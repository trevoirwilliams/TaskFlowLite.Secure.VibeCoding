You are reviewing this .NET 10 ASP.NET Core Web API for security-focused test coverage. 

Review the current TaskFlow Lite API code and the existing test project. Do not write code yet. 

Create a concise test plan for security-focused integration tests around work request access, assignment, status updates, request notes, validation, and response data exposure. 

Focus on tests that prove security assumptions, not just happy-path behavior. For each proposed test, include: 
- Test name
- Endpoint or behavior under test
- Security rule being verified
- Expected HTTP status code or observable result
- Required seed data
- Why the test matters

Avoid suggesting broad architecture changes. Avoid adding unnecessary packages unless the existing test project cannot support integration tests.